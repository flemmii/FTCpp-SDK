#pragma once

namespace Guest1Driver
{
    /*
     * This is NOT a comment. It tells the compiler how to create the java files
     * @TeleOp(name = "Spielemesse 1 Driver", group = "Guest")
     */
    extern "C" void Guest1Driver();
}