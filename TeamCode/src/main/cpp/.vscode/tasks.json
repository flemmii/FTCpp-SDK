{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Download",
            "type": "process",
            "command": "echo",
            "args": [
                "Unable to run download.sh on this OS."
            ],
            "windows": {
                "command": "C:\\Program Files\\Git\\bin\\bash.exe",
                "args": [
                    "${workspaceFolder}/download.sh"
                ]
            },
            "linux": {
                "command": "/bin/bash",
                "args": [
                    "${workspaceFolder}/download.sh"
                ]
            }
        }
    ]
}